hydra:
  run:
      dir: /Users/nsofroniew/Documents/data/rosa/outputs/${adata.embedding_type}/${now:%Y-%m-%d}/${now:%H-%M-%S}
paths:
  adata: /Users/nsofroniew/Documents/data/multiomics/cell_census/tabula_sapiens_pbulk_embeds.h5ad
  chkpt: null
params:
  batch_size: 16384
  learning_rate: 0.001
defaults:
  - adata: joint
  - model: bilinear