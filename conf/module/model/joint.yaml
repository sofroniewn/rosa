dropout_prob: 0.5
layer_norm: True
layer_norm_1: True
criterion:
  loss_function: mse
input_embed:
  layer_norm: True
  dropout_prob: 0.5
  embedding_dim: 256
input_embed_1:
  layer_norm: True
  dropout_prob: 0.5
  embedding_dim: 256
join_embeds:
  method: attention
  out_dim: 256
defaults:
  - expression_head: standard
  - feed_forward: standard