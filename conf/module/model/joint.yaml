dropout_prob: 0.5
layer_norm: True
layer_norm_1: True
input_embed:
  layer_norm: True
  dropout_prob: 0.5
  embedding_dim: 128
input_embed_1:
  layer_norm: True
  dropout_prob: 0.5
  embedding_dim: 128
join_embeds:
  method: bilinear
  out_dim: 64
feed_forward:
  hidden_dim: 256
  dropout_prob: 0.5  
defaults:
  - expression_head: standard