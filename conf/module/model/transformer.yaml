dropout_prob: 0.2
layer_norm: null
layer_norm_1: True
n_bins: ${data_module.data.expression_transform.n_bins}
transformer:
  dim: 128
  heads: 4
  dim_head: 32
  depth: 4
  dropout: 0.2
input_embed:
  layer_norm: True
  dropout_prob: 0.2
  embedding_dim: ${module.model.transformer.dim}
input_embed_1:
  layer_norm: True
  dropout_prob: 0.2
  embedding_dim: ${module.model.transformer.dim}
expression_head:
  projection: True
  activation: null
  likelihood: null
  library_size: null
  n_bins: ${data_module.data.expression_transform.n_bins}
